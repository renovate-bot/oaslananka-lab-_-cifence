name: Multi Document

on: push

permissions:
  contents: read

jobs:
  test:
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      - run: echo safe
---
name: Ignored Document

permissions: write-all
