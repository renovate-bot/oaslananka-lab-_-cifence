name: Service Container Latest

on: push

permissions:
  contents: read

jobs:
  test:
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    services:
      redis:
        image: redis:latest
    steps:
      - run: redis-cli --version
