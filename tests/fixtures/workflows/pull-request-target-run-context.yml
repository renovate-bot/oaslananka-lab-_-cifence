name: Pull Request Target Run Context

on:
  pull_request_target:

permissions:
  contents: read

jobs:
  inspect:
    permissions:
      contents: read
    runs-on: ubuntu-24.04
    steps:
      - run: echo "${{ github.event.pull_request.title }}"
