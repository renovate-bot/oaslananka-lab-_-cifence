name: Workflow Call Secrets Inherit

on: workflow_dispatch

permissions:
  contents: read

jobs:
  call:
    permissions:
      contents: read
    uses: owner/reusable/.github/workflows/deploy.yml@0123456789abcdef0123456789abcdef01234567
    secrets: inherit
